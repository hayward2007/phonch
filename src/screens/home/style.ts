import color from '@styles/color';
import typogrpahy from '@styles/typography';

export default class Style {
    static container = {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    };
    static text = {
        fontSize: 20,
        fontWeight: 'bold',
    };

}